# Handoff — Plantaroo 2.0 upgrade build

**For the next Claude Code session.** Start by reading this file, then [upgradeplan.md](upgradeplan.md) (the authoritative phased plan), then continue the work below. Work on branch `ios-app`. Commit per phase, run `npm --prefix app run typecheck` before each commit.

## Standing decisions (do not re-litigate)

- **iOS app only.** Jamal has fully migrated off the web app (`index.html`) — it is retired; never propose changes to it. All work targets `app/` (Expo SDK 54, RN 0.81, React 19, TypeScript strict).
- **Strictly $0 to run.** No metered API calls ever. AI vision is out of scope. Plant knowledge ships as bundled data generated by Claude Code at build time.
- **Ask before any `eas build`** (limited quota). `eas update` (OTA) and `eas submit` are fine. Phases 1–4 ship OTA; only Phase 5 needs a build.
- `npm install --legacy-peer-deps` always (Expo 54 + React 19).
- Platform note: this machine is Windows; repo at `E:\Downloads 2026\Claude\Plantaroo`. `app/node_modules` is installed; typecheck works natively.

## State of the work

Task list (also in the session task tracker):

| Phase | Status |
|---|---|
| 0 — Housekeeping | **DONE, committed** (`c002a07`) |
| 1 — UI & usability overhaul | **DONE, committed** (see commit on `ios-app`) |
| 2 — Care tasks (mist/clean/prune/repot-check, winter feed pause) | **NEXT** |
| 3 — Plant knowledge + symptom troubleshooter | pending |
| 4 — Growth journal | pending |
| 5 — iOS platform presence (badge, notif actions, widget, export) | pending — needs EAS build, ask first |

Phase 0 delivered: Apple token revocation end-to-end (`app/src/lib/auth.ts` re-auths with Apple inline during deletion → POSTs authorizationCode to new `POST /api/apple-revoke` in `server/index.js`; returns 501 until Jamal sets `APPLE_TEAM_ID/APPLE_KEY_ID/APPLE_PRIVATE_KEY` on Render — best-effort, never blocks deletion); Google button hidden while client IDs are `PASTE_` placeholders in `app/app.json → extra`; Android `RECORD_AUDIO` removed; `jsonwebtoken` added to root `package.json` (not yet `npm install`ed — Render installs on deploy; nothing local needs it).

**Not yet pushed** — `git push origin ios-app` when convenient.

## Phase 1 — DELIVERED (design below kept for reference)

All six items implemented as specced, all JS-only/OTA-able. New files: `app/src/screens/CareQueueScreen.tsx` (route `CareQueue`, fullScreenModal), `app/src/ui/Onboarding.tsx` (overlay in `App.tsx`, flag `plantaroo:onboarded`), `app/src/ui/Skeleton.tsx`. Modified: `PlantCard.tsx` (swipe-right-to-water, layout/entering animations, 56px avatar), `PlantDetailScreen.tsx` (due ring + stat strip), `ToDoScreen.tsx` ("Water all · N" queue entry, pull-to-refresh, skeletons), `PlantsScreen.tsx` (same polish), `useWater.ts` (optional `WaterCallbacks` — `onWatered`/`onCancel`/`silent`, used by the queue), `db.ts` (`isHydrated()`, `refreshFromCloud()`). Visual verification on device still pending — ask Jamal to run it.

1. **Care queue** — new `app/src/screens/CareQueueScreen.tsx`, route `CareQueue` in `RootStackParamList` (full-screen, slide_from_bottom). Snapshot the due list on mount (overdue + due-today + never-watered ids). One plant at a time: big `PlantAvatar` (~140), name, due text, feed/distilled hints; primary Water button, secondary Skip (uses `skipPlant`) and "Later" (advance without action). Progress line "2 of 7" + thin bar. On water: reuse the late-prompt logic from `useWater.ts` (Still wet / Too busy Alert), then a satisfying check animation (Reanimated spring + success haptic) before advancing. End state: "All done — n plants watered" + Done. Entry point: a prominent "Water all · N" row/button on ToDoScreen when dueCount > 0. Reschedule notifications after each water (cheap) or once at end.
2. **Swipe-right-to-water** on `app/src/ui/PlantCard.tsx` — `Gesture.Pan()` from react-native-gesture-handler (installed), `activeOffsetX(20)` + `failOffsetY([-12,12])`, right-swipe only. Card translates; droplet revealed on the left scales in toward threshold (~88px); haptic detent on crossing (useAnimatedReaction + runOnJS); release past threshold → `runOnJS(onWater)()` + spring back. Disabled when `wateredToday`. Keep the water button.
3. **Plant detail upgrades** (`PlantDetailScreen.tsx`) — add a **due-progress ring** (SVG circle, ~6px stroke) around the 104px hero avatar showing elapsed/effective-interval, colored by `dueColor` logic; add a horizontal **stat strip** under the name (interval days · moisture · light · feed cadence) as quiet chips. Keep everything else.
4. **Motion & polish** — Reanimated `layout={LinearTransition}` + `entering={FadeInDown}` on cards in ToDo/Plants lists so watering/reordering animates; **pull-to-refresh** on ToDo + Plants (`RefreshControl`) calling a new exported `refreshFromCloud()` in `app/src/data/db.ts` (wraps `loadAllFromCloud` + persist + emit); **skeleton rows** while first hydrate pending — add `isHydrated()` flag to db.ts, show 3 pulsing placeholder rows when `!hydrated && plants.length === 0`.
5. **Onboarding** — full-screen overlay component (not a nav screen; render inside `ToastProvider` in `App.tsx` when signed in, plants empty after hydrate, and AsyncStorage flag `plantaroo:onboarded` unset). 3 horizontally-paged slides: (a) watering that learns from real behavior, (b) quiet by default / care queue, (c) 350+ plants built in & offline. CTA "Add your first plant" → set flag, `navigationRef.navigate('AddPlant')`. X/skip sets flag too. NOTE: notification permission is ALREADY staged correctly (asked after first plant added, in `AddPlantScreen.save()`) — don't change that.
6. **Empty-state copy pass** — minor refinements only; components exist (`EmptyState.tsx`).

Avatar size on cards: bump 48 → 56 if it looks balanced; not critical.

## Codebase facts (already verified by reading — don't re-explore)

- **Structure:** screens in `app/src/screens/`, primitives in `app/src/ui/` (PressableScale, PlantAvatar, Segmented, SelectRow, OptionSheet, DateSheet, Toast w/ undo, EmptyState, ScreenHeader, icons.tsx — Feather-style SVG set incl. Droplet, Check, Sprout, Clock, Bell), logic in `app/src/logic/` (schedule.ts = core algorithm, actions.ts = mutations returning undo tokens, notify.ts, fertilize.ts, profiles.ts ~350 entries, constants.ts), data in `app/src/data/db.ts`.
- **db.ts:** in-memory cache + Firestore + AsyncStorage mirror (`plantaroo:cache:{uid}`). Reads sync via `getPlants()/getHistory()`; ALL writes via `dbPut/dbAdd/dbDelete`; `subscribe()` powers `usePlants()/useHistory()/usePlant(id)` hooks in `app/src/ui/hooks.ts`. `startSession` hydrates disk then background cloud refresh. Integer ids via `genId()`.
- **Watering flow:** `useWaterAction()` in `app/src/ui/useWater.ts` (late Alert → `waterPlant` → undo toast → `rescheduleWateringReminders(getPlants())`). `actions.ts` also has `skipPlant`, `repotPlant`, `setLastWatered`, `bulkSetLastWatered`, `patchPlant`, `saveNotes`, `removeHistoryEntry`/`recomputeWateringState` (rebuilds schedule from history).
- **schedule.ts:** `getDaysUntilDue` returns -Infinity for never-watered; `getDueText` gives status `'never'|'overdue'|'today'|'soon'|'later'`; clamp 0.5–2× soil-adjusted baseline; seasonal multiplier display-time only.
- **Navigation:** native stack (SignIn / Tabs / PlantDetail / AddPlant / Settings / UpdateRequired) + 3 bottom tabs via custom `TabBar.tsx`. `navigationRef` in `app/src/navigation/ref.ts` for imperative nav (quick actions use it).
- **App.tsx:** auth gate, forced-update gate, quick-actions bridge, ToastProvider wraps NavigationContainer. Notification permission deliberately NOT requested at launch.
- **Photos:** `app/src/lib/photo.ts` — 400px JPEG q0.6 base64 data-URI stored in the plant doc.
- **Theme:** `app/src/theme.ts` — dark-only, tokens for colors/radius/spacing/font/shadows, `dueColor()` helper. Design rules: no pills, no emoji, SVG icons, weights ≤700, quiet.
- **Types:** `app/src/types.ts` — `Plant`, `HistoryEntry` (`type: 'Watered'|'Watered + Fed'|'Skipped'|'Repotted'`, `lateReason`), `Store = 'plants'|'history'|'profileCache'`.
- Gesture-handler v2.28 and Reanimated v4 installed; `GestureHandlerRootView` already at root. `@gorhom/bottom-sheet`, `expo-blur`, `expo-linear-gradient`, FlashList available.

## Phases 2–5 — follow upgradeplan.md, plus these implementation notes

- **Phase 2:** new optional Plant fields (`mist_every_days`, `last_misted`, `clean_every_days`, `last_cleaned`, `last_pruned`) — extend `types.ts`, add task-due computation in a new `logic/tasks.ts` (computed, never stored), actions (`mistPlant` etc. modeled on `skipPlant`), new HistoryType values `'Misted'|'Cleaned'|'Pruned'`, history filter chips, secondary task rows on cards + care queue, misting/cleaning defaults added to `profiles.ts` entries (extend the profile type with optional fields so existing rows stay valid), winter feed pause in `fertilize.ts` (Nov–Feb natural light → double `feed_every_n_waterings`, computed at display time). Include new tasks in the 9am notification digest (`notify.ts`).
- **Phase 3:** new `logic/careInfo.ts` keyed by the same lookup names as `profiles.ts` (light_needs, humidity_pref, difficulty, pet_toxic, temp_range, care_notes — generate content for the ~260 exact-match species; pattern fallbacks get genus-level info). Care section + pet-toxicity warning (subtle red text) in PlantDetail; difficulty hint in AddPlant; new `logic/troubleshoot.ts` rule-based decision tree (symptom → 2–3 follow-ups → diagnosis informed by the plant's real watering data, e.g. compare `getClampedInterval` vs baseline) + a Troubleshoot screen reachable from PlantDetail.
- **Phase 4:** `users/{uid}/journal` subcollection — add `'journal'` to `Store` type + db.ts cache/mirror/deleteAllUserData, entry `{id, plant_id, date, photo, note}`, photos can be ~800px (reuse photo.ts with a size param). Timeline strip in PlantDetail, then-vs-now, "Add a growth photo?" prompt after repot/30 days since last entry. Update `firestore.rules` (owner-only, same pattern as other stores — likely already covered by the `users/{uid}/**` wildcard; verify) and deploy via `firebase deploy --only firestore:rules`.
- **Phase 5:** badge count via `expo-notifications setBadgeCountAsync` on every reschedule (OTA-able, can ship earlier); notification categories with "Mark watered"/"Snooze 1 day" actions handled in a response listener; WidgetKit widget via `@bacons/apple-targets` (Swift target reading App-Group-shared JSON written on every reschedule); verify quick actions; JSON/CSV export from Settings via `expo-sharing` (installed). **Ask Jamal before the EAS build.**

## Verification & deploy

- `npm --prefix app run typecheck` must pass before every commit.
- No test suite exists; for visual verification ask Jamal to run on device, or `npx expo start --dev-client` (needs his dev build).
- Ship phases via `eas update --branch production -m "..."` from `app/` — but coordinate with Jamal; he may want to TestFlight first since v1.0.0 build 1 may not be on the store yet.
- Firestore rules changes: `firebase deploy --only firestore:rules` from repo root.
